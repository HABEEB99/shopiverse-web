import { LoginForm } from "@/components";

const LoginPage = () => {
  return (
    <div className="w-screen wrapper flex items-center justify-center">
      <LoginForm />
    </div>
  );
};

export default LoginPage;
